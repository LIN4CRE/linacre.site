# Copy to `.env` for local use. NEVER commit the real .env — it's gitignored.
# In CI/production these live as GitHub Actions secrets and Vercel env vars, not here.
# Claude uses CLAUDE_CODE_OAUTH_TOKEN (subscription quota) — never set ANTHROPIC_API_KEY.

OPENAI_API_KEY=
GEMINI_API_KEY=
